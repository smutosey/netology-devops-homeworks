---
all:
  children:
    clickhouse:
      hosts:
%{ for i, v in clickhouse ~}
        ${i}:
          ansible_host: ${v}
%{ endfor ~}

    vector:
      hosts:
%{ for i, v in vectors ~}
        ${i}:
          ansible_host: ${v}
%{ endfor ~}